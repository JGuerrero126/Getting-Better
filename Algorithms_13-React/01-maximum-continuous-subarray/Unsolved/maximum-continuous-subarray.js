const maxSumArray = function (numbers) {
  console.log(numbers);
  var j = 0;
  var array1 = [];
  for (i = 0; i < numbers.length; i++) {
    if (numbers[i] + 1 === number[i + 1]) {
      console.log("NUMBER PUSHED TO ARRAY");
      array1.push(numbers[i]);
      console.log(array1);
    }
  }
};
